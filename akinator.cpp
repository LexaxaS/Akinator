#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <string.h>
#include <ctype.h>
#include "tree.hpp"
#include "akinator.hpp"

error_t mainakin(char* filename)
    {
    char cmd[CMD_MAX_LEN] = "";
    size_t scanlen = 0;
    scanf("%s%n", cmd, &scanlen);
    while (cmd != "q")
        {

        scanf("%s%n", cmd, &scanlen);
        }


    }



